# ScreenBridge v8 Release Notes

ScreenBridge v8 focuses on the Mac-to-iPad extended display workflow.

## Highlights

- User-facing app name and packaging are now ScreenBridge.
- New connected-device cleanup: duplicate records are merged, hidden devices can be shown, and saved devices can be removed.
- New default display placement: the virtual display is placed to the right of the Mac display by default.
- New display placement setting: choose right, left, above, or below before connecting.
- New iPad disconnected state: the iPad shows "Device disconnected" when sharing stops or the network drops.
- New larger-text HiDPI resolution: `1024 x 768 HiDPI (Larger Text)`.
- Protocol v2 separates the main media/control session from the recoverable Chrome-audio transport and makes Reset Pairing an immediate revocation boundary.
- Media heartbeat, access-unit, decoder, and renderer health are tracked independently; TCP video, Chrome audio, and heartbeat delivery are bounded, with adaptive video bitrate.
- Protocol v3 adds timed/numbered AAC packets, an eight-packet sender FIFO, and five-packet receiver rebuffering after underruns; audio transport work no longer passes through the main queue.
- The iPad receiver is now an iPad-only, scene-based app while preserving all iPad orientations and windowed presentation.

## Build Notes

- Disposable Mac local-test builds use `ALLOW_AD_HOC=1 ./make_app.sh`; normal packaging requires an Apple-issued identity.
- Mac and iPad app version metadata are aligned on ScreenBridge `8.0` / build `8`.
- iOS IPA creation requires an Xcode archive/export options plist; the script no longer hand-assembles an incomplete Payload.
- Public distribution should use your own Developer ID certificate and notarization credentials.
- Generated app, DMG, IPA, zip, and `dist/` artifacts are release assets, not committed source files.
